# Stage228R9 R4 - CaseDetail left header guard repair

- date: 2026-06-08 17:15 Europe/Warsaw
- repo: dkknapikdamian-collab/leadflowv1
- branch: dev-rollout-freeze
- issue:
  - R9R3 applied the intended layout patch but failed on its own guard.
  - Guard incorrectly forbade the old R9 wide-header CSS block even though R9R3 intentionally overrides it later.
- fix:
  - Keep R9R3 layout.
  - Replace R9R3 guard with a correct contract check.
  - Guard now requires R9R3 grid/header/rail/tabs selectors and no longer forbids compatible R9 CSS.
- tests:
  - node scripts/check-stage228r9r3-case-detail-left-header-rail-top-grid.cjs
  - node scripts/check-stage228r9-case-detail-shell-rail-lift.cjs
  - npm run build
- manual test:
  - Open CaseDetail.
  - Header should end before the right rail.
  - Right rail should start at the same top row as the header.
  - Tabs should be centered in the left/main area.
- risk audit:
  - No data, SQL or finance change.
  - Guard-only repair plus verification of partially applied R9R3.
  - Keeps R9R3 CSS override instead of removing older compatibility CSS.
