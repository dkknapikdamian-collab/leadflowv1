# Stage228R9 R3 - CaseDetail left header and rail top grid

- date: 2026-06-08 17:00 Europe/Warsaw
- repo: dkknapikdamian-collab/leadflowv1
- branch: dev-rollout-freeze
- input:
  - Damian clarified that the header should not span under the right rail.
  - Header/card with case/client data should end where the left content column ends.
  - The right rail should move to the very top, beside the header.
  - The three tabs should sit centered in the main/left area.
- scan:
  - R9R2 moved Tabs into main column and full-width header remained above the shell.
  - CaseDetail currently has header before shell, then shell with main column and right rail.
  - CSS can solve this by making the page a two-column grid and the shell display: contents.
- fix:
  - Page becomes a two-column grid.
  - Header goes into grid column 1 row 1.
  - Main column goes into grid column 1 row 2.
  - Right rail goes into grid column 2 row 1 / span 2.
  - Tabs are centered in the left column.
  - Added guard scripts/check-stage228r9r3-case-detail-left-header-rail-top-grid.cjs.
- tests:
  - node scripts/check-stage228r9r3-case-detail-left-header-rail-top-grid.cjs
  - npm run build
- manual test:
  - Open CaseDetail.
  - Header should end before the right rail.
  - Right rail should start at the same top row as the header.
  - Tabs should be centered in the left/main area.
- risk audit:
  - No data or SQL change.
  - Layout only.
  - Uses CSS display: contents on shell; DOM semantics remain but layout box is removed.
  - Responsive fallback returns to block/grid below 1220px.
