# Stage114F - Calendar mojibake guard self-cleanup local-only

## Status
LOCAL-ONLY / DO WDROZENIA BEZ PUSH.

## Problem
Stage114E focused guards and build passed, but full quiet gate failed on Stage98 mojibake scan.

The failing files were generated by the Stage114E package itself:
- `scripts/check-stage114-calendar-mojibake.cjs`
- `_project/runs/2026-05-18_stage114e_calendar_p0_batch_local_only.md`

The new check and note contained forbidden mojibake glyphs as examples. The guard correctly rejected them.

## Decision
Do not weaken Stage98. Clean the generated script and generated run note so forbidden glyphs are represented by ASCII codepoint names only.

## Scope
- Rewrite `scripts/check-stage114-calendar-mojibake.cjs` without literal forbidden glyphs.
- Sanitize the Stage114E run note if it exists locally.
- Keep Stage114E source changes local-only.
- No commit.
- No push.

## Tests
- `node scripts/check-stage114-calendar-mojibake.cjs`
- `node --test tests/stage98-polish-mojibake-calendar-guard.test.cjs`
- Stage114 hard refresh guard
- Stage114 shift persistence guard
- Stage114 modal viewport guard
- Stage116 dialog description regression guard
- `npm run build`
- `npm run verify:closeflow:quiet`

## Manual QA after green tests
- Hard refresh `/calendar`.
- Check entries are visible without leaving and returning.
- Test `+1D` and `+1W` on a task.
- Open Calendar create/edit modals.
- Console without Radix description warning.
