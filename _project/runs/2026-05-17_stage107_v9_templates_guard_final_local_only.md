# Stage107 V9 - templates delete visual guard final local-only

## Status
LOCAL ONLY. No git add, commit or push was executed by this package.

## Facts
- Scope: /templates only. Calendar work is intentionally separate.
- V9 fixes the malformed Stage107 guard generated by V8.
- Delete action remains based on EntityTrashButton and trashActionIconClass.
- Delete handler requires confirmation and a second confirmation when checklist items exist.
- Guard: tests/stage107-templates-delete-and-visual-contract.test.cjs.

## Tests run by package
- node --test tests/stage107-templates-delete-and-visual-contract.test.cjs
- npm run build

## Tests to be performed by the user after push
1. Open /templates.
2. Confirm that template cards use the same bright readable visual language as the rest of CloseFlow.
3. Confirm that every template card has a visible Usun button.
4. Click Usun and cancel the first confirmation. The template must remain.
5. On a test template, click Usun and confirm deletion.
6. If the template has checklist items, confirm the second checklist warning.
7. Confirm that the template disappears from the list.
8. Refresh the page and confirm that the deleted template remains gone.

## Rule carried forward
Every local-only stage prepared before push must include Tests to be performed by the user after push in _project and Obsidian.
