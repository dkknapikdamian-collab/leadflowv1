# Stage228R51 â€” R50 guard repair and no-flicker continuation

Date: 2026-06-09 09:05 Europe/Warsaw
Branch: dev-rollout-freeze

## Facts
- R50 patch reached runtime files but failed on a brittle guard token: `setLinkedTasks((previous) => previous.some`.
- R49B scan proved the real LeadDetail anchors are `linkedTasks/setLinkedTasks` and `linkedEvents/setLinkedEvents`.
- R51 replaces the brittle exact-format guard with real-anchor checks and keeps the R50 runtime changes under build/test verification.

## Tests required
- `node scripts/check-stage228r47-sql-deleted-status-constraint.cjs`
- `node scripts/check-stage228r50-no-flicker-real-anchors.cjs`
- `node scripts/check-stage228r51-r50-guard-repair.cjs`
- `node --test tests/stage228r47-sql-deleted-status-constraint.test.cjs`
- `node --test tests/stage228r50-no-flicker-real-anchors.test.cjs`
- `node --test tests/stage228r51-r50-guard-repair.test.cjs`
- `npm run build`
- `git diff --check`
- `git diff --cached --check`

## Manual test after deploy
Create `CF_DEL_TEST_4` on lead detail, delete it, verify it disappears without full-page flicker and stays deleted after refresh.

## Risk audit
- The local optimistic UX must not hide backend failures permanently; rollback/error toast must remain in delete handlers.
- The mutation bus must not use object ids; R51 guards scalar `input.id` for soft delete events.
- Calendar remains the baseline and is not intentionally refactored here.
