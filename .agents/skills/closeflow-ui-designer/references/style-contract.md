# Style Contract Seed

## Metric tiles

Owned by:

- `src/components/StatShortcutCard.tsx`
- `src/styles/closeflow-metric-tiles.css`

## Product actions

Base component:

- `src/components/ui/button.tsx`

Needed layer:

- product/entity action contract for repeated action semantics.

## CSS ownership rule

Global repeated visual meaning belongs in a global style/token file or shared component. Page-specific CSS may only arrange page composition or truly unique layout.
