# Final push V21 - preflight first

- Native command stderr warnings are tolerated when exit code is 0.
- Logs/backups are stored outside the repo.
- Stable contract guards replace brittle marker-only guards.
- Suspicious runtime files are intentionally excluded from the commit.
